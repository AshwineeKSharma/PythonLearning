from typing import Type


# Assigning a variable with empty value declares it as a dictionary but not empty set.

s1={}
print(type(s1))

# To define(Declare) the empty set we require the following Syntax:

s2= set()
print(type(s2))

